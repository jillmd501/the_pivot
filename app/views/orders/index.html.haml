%h1.center-align.amber-text My Orders

.container
  %table.orders.highlight.centered
    %thead
      %tr
        %th
          Order Number
        %th
          Status
        %th
          Order Date
        %th
          Total Cost
        %th
          Cancel?
    %tbody
      - @orders.each do |order|
        = render partial: "order", locals: { order: order }
